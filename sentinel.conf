# specify path to vircles.conf or leave blank
# default is the same as VirclesCore
# vircles_conf=/root/.vircles/vircles.conf
vircles_conf=/opt/.vircles/vircles.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
